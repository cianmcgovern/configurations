export SHELL=/bin/zsh
export EDITOR=/usr/bin/vim

bindkey "^[[1~" beginning-of-line
bindkey "^[[4~" end-of-line

setopt HIST_IGNORE_SPACE

if [ -f /home/cian/.exp.local ];
then
    source /home/cian/.exp.local
fi
